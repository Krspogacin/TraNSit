<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.FavouriteLocationsActivity"
    android:orientation="vertical">

    <TextView
        android:id="@+id/all_favourite_locations_list_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:fontFamily="sans-serif"
        android:layout_marginStart="20dp"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="12dp"
        android:textColor="#51000000"
        android:text="@string/favourite_locations_header" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/all_favourite_locations_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    </androidx.recyclerview.widget.RecyclerView>
</LinearLayout>